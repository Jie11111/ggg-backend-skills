interface:
  display_name: "GGG Code Review"
  short_description: "优先 fresh context，分两门检查完整差异并披露评审方式"
  default_prompt: "使用 $ggg-code-review 优先通过 fresh-context 评审当前完整差异，不可用时显式降级 self-review，再完成 Gate A、Gate B 和结论绑定。"
