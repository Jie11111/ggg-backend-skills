interface:
  display_name: "GGG Requirement Alignment"
  short_description: "需求对齐：验证代码、共享语义与 SQL，确认后再设计"
  default_prompt: "使用 $ggg-requirement-alignment 基于已确认基线验证主链路、共享语义、跨项目依赖及新增或修改 SQL，并在用户确认 SQL 后收口。"
