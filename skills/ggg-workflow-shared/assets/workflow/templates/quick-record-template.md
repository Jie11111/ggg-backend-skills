<!-- GGG_QUICK_SCHEMA_VERSION: 4 -->
# Quick 小需求记录：{{quick_name}}

> 用途：记录 quick 小需求的边界、实现摘要和验证结论。quick 与 full 共用 `ggg/features` 目录；是否进入 full 以同目录是否存在 `meta.json` 为准。quick 不要求 `00-07` 全套阶段文档。

## 1. 边界确认

- 推进模式：quick
- 推荐模式：{{recommended_mode}}
- 推荐依据：{{recommendation_reason}}
- 最终模式：quick
- 模式选择来源：{{selection_source}}
- 澄清状态：澄清中 / 已确认
- 最终边界确认：待确认 / 已确认（记录用户消息或确认时间）
- 一句话目标：
- 改什么：
- 不改什么：
- 预计主项目 / 代码范围：
- 代表性验收例：`前置/输入 -> 用户操作或触发 -> 系统处理 -> 用户可见结果`
- 失败 / 重复触发补充：不涉及 / `失败、重试或再次操作时的预期`
- 兼容性检查：`现有调用方=无影响/有影响/未知；历史数据=无影响/有影响/未知；重复请求或重试=无影响/有影响/未知`
- 最小验收信号：

| 编号 | 疑问 | 影响级别 | 准确来源 | 为什么不确定 | 用户结论 | 状态 |
|---|---|---|---|---|---|---|
| Q1 |  | 高影响阻塞 / 低风险 | PRD 章节 / 用户消息 |  |  | 待确认 / 已确认 |

> 没有疑问时删除 Q1 示例行并保留表头，不为填模板制造问题。

## 2. 升级 full 触发条件

- 业务目标、业务实例身份或状态隔离不清。
- 权限边界、旧链路副作用或影响面无法判断。
- 接口契约、SQL 影响面、跨项目契约或验证方式不清。
- 现有调用方、历史数据、重复请求或重试的兼容影响无法判断。
- 实际改动超过 quick 边界，继续做会变成正式需求。

### 2.1 按需统一产物

- SQL：不涉及 / `04-schema.sql`
- 接口说明：不涉及 / `interface-details/02-interface-NN-主题.md`
- 产物约定：quick 与 full 使用相同文件名和目录；创建这些产物不代表自动升级 full。

## 3. 实现摘要

> 修改文件使用仓库相对路径；涉及多个 Git 仓库时使用 `仓库标签/相对路径`，路径含空格时用反引号包裹。

- 实现状态：未开始 / 编码中 / 已完成 / 已失效待重入
- 当前实现轮次：I0
- 风险级别：tiny / normal / high
- 实现会话状态文件：`implementation-state.json`
- 预检状态：未执行 / 通过 / 阻塞 / 已失效
- 预检对应实现轮次：
- 预检记录指纹：
- 涉及 Git 仓库及编码基线：
- 修改文件：
- 核心改动：
- 完成标准证据：
- 是否越界：
- 剩余风险：

### 3.1 编码前实现预检

> `implementation-start` 后、第一处新代码修改前填写并执行 `implementation-precheck`。简单改动每项一两句话即可。

| 轮次 | 检查面 | 预检结论 | 事实依据 | 状态 |
|---|---|---|---|---|
| I | 范围与主链路 |  |  | 通过 / 阻塞 |
| I | 代码落点与职责 |  |  | 通过 / 阻塞 |
| I | 数据、契约与失败边界 |  |  | 通过 / 阻塞 |
| I | 验证策略 |  |  | 通过 / 阻塞 |

## 4. 验证记录

### 4.1 代码质量自检

> 按 quick 实际改动和风险记录综合结论；不重复维护逐文件表和固定十项表。

| 任务/范围 | 自检重点 | 证据（文件行号/命令） | 结论 | 未通过处理 |
|---|---|---|---|---|
| quick 边界 | 代码设计 / 契约 / SQL / 权限 / 事务 / 异常日志 / 性能 / 注释 / 格式测试 |  | 通过 / 未通过 |  |

#### 4.1.1 独立契约与可观测性证据

> 每轮逐项填写；`不涉及`必须写清本次 diff 为什么没有触发该检查面。测试代码默认不修改，只有用户明确授权时才能登记“已授权并修改”。

| 轮次 | 检查项 | 结论 | 独立证据（文件:行号/命令） | 不涉及原因或用户授权 |
|---|---|---|---|---|
| I | ID / 大整数精度 | 通过 / 有问题 / 不涉及 |  |  |
| I | Request / Response / DTO 字段注释 | 通过 / 有问题 / 不涉及 |  |  |
| I | 异常 / 错误码 | 通过 / 有问题 / 不涉及 |  |  |
| I | 业务日志 | 通过 / 有问题 / 不涉及 |  |  |
| I | Trace 链路 | 通过 / 有问题 / 不涉及 |  |  |
| I | 测试代码是否修改及用户授权 | 未修改 / 已授权并修改 / 有问题 |  | 用户消息定位 / 不涉及：未修改测试代码 |

- 关键逻辑注释证据：不涉及（写明本轮没有复杂逻辑的原因） / `本轮修改文件:行号`＋注释解释的顺序、并发或失败边界
- SQL/DDL 规范证据：不涉及 / `本轮SQL文件`＋参考表或统一兜底＋公共字段/例外＋生产方言与测试 schema 区分

### 4.2 运行验证

- 验证命令 / 方式：
- 验证结果：
- 未验证项：
- 阻塞原因：
- 实现完成命令：`workflow_cli.py implementation-complete --record <quick.md>`
- 最终差异指纹：

## 5. 可选 Review / 测试结论

> Review 仅在用户明确要求时执行；未执行 Review 不影响进入测试，保持“未执行”即可。

- Review 状态：未执行 / 已执行
- Review 结论：未执行 / 通过 / 需修改 / 阻塞
- Review 对应实现轮次：
- Review 对应差异指纹：
- Review 未解决问题：无 / 具体问题
- 测试结论：未执行 / 通过 / 需补测 / 阻塞
- 测试对应实现轮次：
- 测试对应差异指纹：
- 测试绑定命令：`workflow_cli.py test-mark --record <quick.md> --result <passed|needs_more|blocked>`
- 最终结论：进行中 / 通过 / 需修改 / 需补测 / 阻塞 / 建议升级 full
- 后续动作：

### 5.1 可选 Review

> 仅在用户明确要求 Review 时填写。

| 检查项 | 结论 | 问题与定位 |
|---|---|---|
| 代码与需求是否有偏差 | 通过 / 有问题 / 阻塞 | 无 / `文件:行号`＋问题、影响和建议 |
| 代码质量与格式 | 通过 / 有问题 / 阻塞 | 无 / `文件:行号`＋问题、影响和建议 |

### 5.2 Quick 测试场景

> 只有按需触发独立测试时填写。`来源依据` 必须定位已确认边界和每个真实 Diff 文件；如果做过可选 Review，再纳入其中尚需验证的问题。通过证据写命令或请求、退出码/协议结果、业务回查和 64 位 SHA-256。高风险或证据较多时升级 full。

| 场景ID | 来源依据 | 业务场景 | 级别 | 前置条件 | 操作与测试数据 | 预期结果 | Effect | 结果 | 证据或原因 |
|---|---|---|---|---|---|---|---|---|---|
| TS1 | 边界目标；diff: `相对路径` |  | 关键 / 一般 |  |  |  | read-only / local-write / data-write / state-change / message-or-job / external-side-effect | 通过 / 失败 / 阻塞 / 未执行 / 不适用 |  |

### 5.3 Quick 命令证据

> 仅由 `test-run` 写入；命令退出码不自动替代业务场景结论。

| 执行ID | 场景ID | 时间 | 命令 / cwd / 环境 | Effect | 机器证据 | SHA-256 | 结论 |
|---|---|---|---|---|---|---|---|
| Exx | TSxx |  |  | read-only |  |  | PASS / FAIL / BLOCKED |
