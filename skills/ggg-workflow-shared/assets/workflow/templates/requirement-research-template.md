# 代码调研模板

<!-- GGG_RESEARCH_SCHEMA_VERSION: 2 -->

> 用途：逐项验证 `00-baseline.md` 中的 Bxx，并记录代码层面的确认结论。每个 Bxx 必须且只能在清单中出现一次；每个关键结论都要进入结论账本并绑定证据 ID，未找到只能写“当前证据范围内未发现”。

## 1. Baseline 验证清单

| baseline ID | baseline 条目 | 需要验证的代码位置 | 验证状态 | 代码事实 | 证据ID | 结论 | 结论ID | 风险 |
|---|---|---|---|---|---|---|---|---|
| B1 |  |  | 已验证 / 部分验证 / 未覆盖 / 阻塞 |  | E1 |  | C1 |  |

## 2. 主链路代码事实

> 按 baseline 的用户闭环，从入口走到最终结果，验证关联产物不变量和重试路径。
> 每一项都必须填写真实结论；确实不涉及时写“`不涉及：原因`”，不能只保留空标签。

- 入口：
- 核心处理与异步链路：
- 关联产物不变量及数据承载：
- 失败重试 / 主动再次操作：
- 框架隐式链路（AOP / Filter / Interceptor / 动态 Bean / 事务 / 异常 / 生成代码）：
- 关键依赖和数据落点：

## 3. 旧链路副作用清单

> 至少填写一条真实业务行；不涉及时在首列写“`不涉及：原因`”。第 4～7 节同样适用。

| 旧能力 | 代码位置 | 现有语义 | 反向影响范围 | 与本需求关系 | 结论 | 结论ID | 说明 |
|---|---|---|---|---|---|---|---|
|  |  |  |  |  | 可直接复用 / 可扩展复用 / 只可参考 / 禁止复用 / 必须新增 | C1 |  |

## 4. 数据身份和状态维度对照

| 业务对象 | 唯一标识 | 去重维度 | 状态隔离维度 | 现有代码/表字段 | 是否满足 | 结论ID | 风险 |
|---|---|---|---|---|---|---|---|
|  |  |  |  |  | 是 / 否 / 部分满足 | C1 |  |

## 5. 复用性分级

| 能力/代码 | 代码位置 | 复用等级 | 原因 | 需要改造点 | 结论ID |
|---|---|---|---|---|---|
|  |  | 可直接复用 / 可扩展复用 / 只可参考 / 禁止复用 / 必须新增 |  |  | C1 |

## 6. 旧能力反向影响检查

| 准备复用/改造的旧能力 | 调用方/影响面 | CodeGraph 证据 | 影响旧场景 | 结论 | 结论ID |
|---|---|---|---|---|---|
|  |  |  |  |  | C1 |

### 6.1 共享状态、枚举和类型语义影响矩阵

> 新增或修改状态、枚举值、类型码、业务阶段、场景标识或既有字段语义时，必须从共享语义反向核对所有生产方和消费场景。不能只沿主入口调用链调研，也不能只搜索枚举类名。

- 影响面判断：涉及 / 不涉及：具体原因
- 检索范围：

| 共享语义 | 权威载体/存储值 | 消费场景 | 读取/传播位置 | 当前处理规则 | 新语义预期 | 代码改动结论 | 证据ID | 结论ID | 验证缺口 |
|---|---|---|---|---|---|---|---|---|---|
|  |  |  |  |  |  | 需改 / 无需改 / 仅运行验证 / 阻塞 | E1 | C1 |  |

## 7. 跨项目依赖能力

| 项目 | Facade / HTTP / MQ / 表 | 当前能力 | 是否满足本需求 | 缺口 | 结论ID |
|---|---|---|---|---|---|
|  |  |  | 是 / 否 / 部分满足 |  | C1 |

## 8. 结论账本（Claim Ledger）

> 这是关键结论、证据覆盖度、运行时缺口和置信度的唯一记录位置，不再另建重复的覆盖度表。

| 结论ID | 关键结论 | 结论类型 | 证据ID | 证据等级 | 置信度 | 未覆盖范围 | 运行时证据缺口 | 若结论错误的影响 | 后续确认方式 |
|---|---|---|---|---|---|---|---|---|---|
| C1 |  | 主链路 / 复用边界 / 旧链路副作用 / 数据身份 / 共享语义影响 / 跨项目依赖 / 技术可行性 | E1 | 代码已证实 / 编译已证实 / 接口已证实 / 数据已证实 / 推断 / 未覆盖 / 阻塞 | 高 / 中 / 低 |  | 配置确认 / 接口验证 / 日志运行时确认 / 测试数据确认 / 用户口径确认 / 无 |  |  |

## 9. 进入技术方案前疑问账本

> 这是需求对齐阶段唯一的疑问和阻塞问题账本。用户可见提问只展示“当前疑问”和“推荐理解”；来源、证据、影响和确认过程只记录在这里。
> Baseline、Claim Ledger 或残余风险若写“阻塞”，必须引用这里的 Qxx；只要任一处仍为阻塞，`validate` 和 `to-design` 都不能通过。

| 编号 | 疑问 | 问题类型 | 准确来源 | 为什么不确定 | 影响范围 | 应由谁确认 | 确认结论/转交说明 | 结论ID | 状态 |
|---|---|---|---|---|---|---|---|---|---|
| Q1 |  | 用户意图 / 代码事实 / 设计选择 | PRD / 用户消息 / Exx 代码证据 |  |  | 用户 / 需求对齐 / 技术方案 |  | C1 | 待确认 / 已确认 / 转下游 |

## 10. 残余风险和后续确认方式

> 这里只保留非阻塞缺口。完成需求对齐时“阻塞风险”必须填写“无”；真实阻塞必须先登记到 Qxx 并清零。

- 已确认：
- 未覆盖：
- 非阻塞风险：
- 阻塞风险：无

## 11. 代码证据索引

> 文件类证据的“位置”必须写仓库根相对路径或绝对路径加行号，例如 `src/main/java/com/demo/ReportController.java:42`。`validate` 会检查文件存在和行号范围；非文件运行证据需在类型中写明为运行时 / 接口 / 日志 / 数据证据。

| 编号 | 项目 | 类型 | 位置 | 结论说明 |
|---|---|---|---|---|
| E1 |  | Controller / Service / Mapper / Facade / DTO / 配置 / SQL | src/main/java/.../Xxx.java:42 |  |
